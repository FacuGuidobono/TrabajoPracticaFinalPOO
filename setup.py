from setuptools import setup, find_packages

setup(name = 'TrabajoPracticaFinalPOO', version= 1.0 , packages=find_packages())

#if have the error: ModuleNotFoundError: No module named 'clases'
#run in terminal: 'pip install -e .'
